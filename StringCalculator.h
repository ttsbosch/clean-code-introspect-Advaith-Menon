#include<iostream>
#include<string>
using namespace std;
class StringCalculator
{
   public:
   int add(string input);
   void containsNegative(const std::string& input);
   void removeNewLineCharacters(const std::string& input);
};
